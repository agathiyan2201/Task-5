// Shopping Cart (Return)

function cartTotal(price1, price2, price3) {
    return price1 + price2 + price3;
}

// Store the returned value
let totalAmount = cartTotal(500, 1200, 300);

// Print the result
console.log("Total Amount : " + totalAmount);